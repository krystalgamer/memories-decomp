## `func_80037DA4` at 0x80037DA4

`gcc_2_8_1_g8_split`, 160 of 160 instructions, opcode distance 0, 7 differing
positions, all of them the `$v0`/`$v1` pair of the stream-slot pointer and
the current byte pointer in the prologue.

The text-stream card-name command, the function after `func_80037D6C` in
`text_stream_commands.c`. It clears the object's +0x62, reads one opcode
byte from the stream slot selected by the depth at +0x58 and advances it.
Bit 4 copies `D_8009B320` into +0x54 and returns. Bit 5 or bit 6 forms a
string id from `gDuel_wSelectedCardID` plus 0x8000 or 0xD100. Otherwise the
low nibble selects a field of the selected card's `gDuel_adwCardStats` word:
0 takes bits 26 to 30, 1 takes bits 22 to 25 plus 0x17 and records bits 26
to 30 into +0x62 when they are 0x14 to 0x17, 2 takes bits 18 to 21 plus 0x17
and sets the flag when that is 0x17; without bit 7 the object's +0x34 gets
bit 7 set, `func_80036C14(object, id)` runs unless the flag is set, and the
bit is cleared and +0x38 advanced by 0x10 instead of pushing. With bit 7 the
id gains 0x8300 and the depth is incremented; ids above 0xCFFF index
`D_801C0000` from 0xD000, ids above 0x7FFF index `D_801D5800` from 0x8000,
smaller ones index `D_801C0000` directly (less 0x100 from 0x500 up) against
the `D_801B0000` segment, and the resulting string pointer is stored into the
new slot.

Six canonical attempts, all at 232 to 256 bytes against 640. This is the
first candidate at the exact size and multiset.

**What the shape needed.**

- *The `gcc_2_8_1_g8_split` profile, not the neighbour's `g8`.* Every
  `gDuel_adwCardStats[card - 1]` access in retail materialises the array
  address into a register before the index arithmetic; under non-split `g8`
  the backend accepts `reg + symbol` addresses and every spelling folds the
  base into an assembler `$at` macro. The other matched users of that array
  are all split-profile.
- *`func_80036C14(object, id)` with two arguments.* Retail's call sets only
  `$a0` because `id` is already in `$a1`. Passing it is what keeps `id`
  canonical in cse (`make_regs_eqv` makes the copy's destination canonical
  only when it outlives the source by last mention) and what gives `id` the
  `$a1` preference; with a one-argument call the compares in the push block
  come out on the copy's register.
- *`n = id` before the table dispatch,* with `n` also the case-2 flag that
  the plain path tests as `(u8)n`. The copy then lands in the first branch's
  delay slot as retail has it.
- *Case 2 builds `id` in two statements* (`id = ...; id += 0x17;`) so
  combine cannot fold `(x + 0x17) == 0x17` to `x == 0`; case 1 already had
  that shape.
- *The two big-table branches each write the full expression.* jump.c
  cross-jumps the common suffix after the constants, so the mask is
  materialised in both predecessors and only the index and store code is
  shared, which is retail's `.L80037F70` layout.
- *The plain path is a `goto` to a label after the store,* which is what puts
  it out of line at the end; an inline `if` body lands before the push code.
- *The switch is a `switch`.* The 0/1/2 decision tree with `op & 0x80`
  hoisted into the delay slots is GCC's own lowering.

**Where the residual is.** Retail keeps the slot pointer in `$v1` and the
current pointer in `$v0`, as the matched neighbour `func_80037D6C` does with
the same source idiom. Here the byte just read feeds the first `if (op &
0x10)`, and with that branch present local-alloc gives the pair the other way
round: probes show the flip appears the moment a branch tests the byte's
register and disappears when the branch tests anything else. Crossed without
moving it: every read-and-advance spelling, `u8` and `s32` for the byte,
declaration order, a depth local, the clear byte before or after the read,
the byte re-read for the test, and pins on either the slot pointer or the
byte.

```c
#include "../types.h"

extern u8 D_8009B320;
extern s16 gDuel_wSelectedCardID;
extern s32 gDuel_adwCardStats[];
extern u16 D_801B0000[];
extern u16 D_801C0000[];
extern u16 D_801D5800[];

extern void func_80036C14(u8 *object, s32 id);

void func_80037DA4(u8 *object)
{
    u8 **stream;
    u8 *current;
    s32 op;
    s32 id;
    s32 n;
    s32 kind;
    s32 stats;
    s32 type;
    u8 *text;
    u8 **slot;

    stream = &((u8 **)object)[*(s8 *)(object + 0x58)];
    object[0x62] = 0;
    current = *stream;
    op = current[0];
    *stream = current + 1;
    n = 0;
    if (op & 0x10) {
        object[0x54] = D_8009B320;
        return;
    }
    if (op & 0x20) {
        id = gDuel_wSelectedCardID + 0x8000;
    } else if (op & 0x40) {
        id = gDuel_wSelectedCardID + 0xD100;
    } else {
        kind = op & 0xF;
        id = 0;
        switch (kind) {
        case 0:
            id = (gDuel_adwCardStats[gDuel_wSelectedCardID - 1] >> 26) & 0x1F;
            break;
        case 1:
            stats = gDuel_adwCardStats[gDuel_wSelectedCardID - 1];
            id = (stats >> 22) & 0xF;
            type = (stats >> 26) & 0x1F;
            id += 0x17;
            if ((u32)(type - 0x14) < 4) {
                object[0x62] = type;
            }
            break;
        case 2:
            id = (gDuel_adwCardStats[gDuel_wSelectedCardID - 1] >> 18) & 0xF;
            id += 0x17;
            if (id == 0x17) {
                n = 1;
            }
            break;
        }
        if (!(op & 0x80)) {
            goto plain;
        }
        id += 0x8300;
    }
    object[0x58]++;
    n = id;
    if (id > 0xCFFF) {
        text = (u8 *)((u32)D_801C0000 & 0xFFFF0000) + D_801C0000[id - 0xD000];
    } else if (id > 0x7FFF) {
        text = (u8 *)((u32)D_801D5800 & 0xFFFF0000) + D_801D5800[id - 0x8000];
    } else {
        if (id >= 0x500) {
            n = id - 0x100;
        }
        text = (u8 *)((u32)D_801B0000 & 0xFFFF0000) + D_801C0000[n];
    }
store:
    slot = &((u8 **)object)[*(s8 *)(object + 0x58)];
    *slot = text;
    return;
plain:
    *(u16 *)(object + 0x34) |= 0x80;
    if ((u8)n == 0) {
        func_80036C14(object, id);
    }
    *(u16 *)(object + 0x34) &= 0xFF7F;
    *(u16 *)(object + 0x38) += 0x10;
}
```
