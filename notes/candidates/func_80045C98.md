## `func_80045C98` at 0x80045C98

`gcc_2_8_1_g0`, 169 of 169 instructions, opcode distance 0, 7 differing
positions, all register names: the two `mult` products come back through
`$a2` (`mflo $a2; andi $v0, $a2`) where retail has `$v0` for both, and the
zero-extended first volume sits in `$v1` for retail's `$a0`.

The per-frame volume fade in the sound driver, next to `func_80045BE8`.
Three fades share one shape: when the step at +0x1588 (+0x1582, +0x512)
is non-zero it is added to the level at +0x1586 (+0x1580, +0x510), and the
level is clamped to the target byte at +0x158A (+0x1584, +0x49) once it
crosses it in the step's direction, clearing the step. The first two set a
changed flag; when it is set the level at +0x1580 is scaled by the mixer
value at +0x44 and then by the level at +0x1586, each as
`(u16)(a * (b + 1)) >> 8`, and the result goes to `func_80049F10` twice.
The third fade ends with `func_80044DC0` on the new CD volume.

**What the shape needed.**

- *Every access through `g_SDValue` as a struct field, no cached pointer.*
  The halfword `+=` compiles to `lhu; lhu; addu; sh` on its own, and the
  clamp test folds the stored sum back through `sll`/`sra`. The pointer
  reloads at the join labels come from cse's table flush, and the
  branch-into-the-middle jumps (`.L80045D44`) from cse retargeting a
  branch past a reload it can prove redundant.
- *`changed = 1` right after the add, inside the step test,* so it lands
  in the branch delay slot.
- *The two scaled values are ternaries* (`m ? ... : 0`) on a `u16` mixer
  local read once, a `u16` first result (the `andi 0xFFFF` at the join is
  its promotion) and an `s16` second result.
- *The `s16` result local is also used for the CD volume call.* Its load
  from +0x510 gives the pseudo unknown high bits, which is what keeps the
  `sll`/`sra` sign extension before `func_80049F10`; with the local set
  only from the shifted products combine proves the value fits and folds
  the extension to a move.

**Where the residual is.** The product pseudo of each `mult` has a single
register use, so regclass prefers `LO_REG` for it, local allocation skips
it (`CLASS_LIKELY_SPILLED_P`), global allocation puts it in `LO`, and
reload materialises it through a fresh spill register, the first unused
caller-saved one, `$a2`. Retail's `mflo $v0` needs the product to be an
ordinary `GR_REGS` pseudo, which happens as soon as it has a second
register use (verified with a dummy compare: `mflo $a0`), but no spelling
found gives it a second use without extra instructions. Crossed: `s32`
product locals, one shared and one per block; `(u16)`, `& 0xFFFF` and
`(u32)(u16)` truncations; `u16` intermediates for the truncated product;
if/else instead of the ternaries. The `$a0`/`$v1` choice for the first
result follows the same allocation.

```c
#include "../types.h"
#include "sound.h"

typedef struct {
    u8 pad000[0x44];
    u16 field_0044;
    u8 pad046[3];
    u8 field_0049;
    u8 pad04A[0x510 - 0x4A];
    s16 cd_volume;
    s16 field_0512;
    u8 pad514[0x1580 - 0x514];
    s16 field_1580;
    s16 field_1582;
    u8 field_1584;
    u8 pad1585;
    s16 field_1586;
    s16 field_1588;
    u8 field_158A;
} SDFade;

#define SD ((SDFade *)g_SDValue)

extern void func_80049F10(s16, s16);
extern void func_80044DC0(s16);

void func_80045C98(void) {
    u8 changed;
    u16 m;
    u16 x;
    s16 y;

    changed = 0;
    if (SD->field_1588 != 0) {
        SD->field_1586 += SD->field_1588;
        changed = 1;
        if (SD->field_1586 <= SD->field_158A && SD->field_1588 < 0) {
            SD->field_1588 = 0;
            SD->field_1586 = SD->field_158A;
        }
        if (SD->field_1586 >= SD->field_158A && SD->field_1588 > 0) {
            SD->field_1588 = 0;
            SD->field_1586 = SD->field_158A;
        }
    }
    if (SD->field_1582 != 0) {
        SD->field_1580 += SD->field_1582;
        changed = 1;
        if (SD->field_1580 <= SD->field_1584 && SD->field_1582 < 0) {
            SD->field_1582 = 0;
            SD->field_1580 = SD->field_1584;
        }
        if (SD->field_1580 >= SD->field_1584 && SD->field_1582 > 0) {
            SD->field_1582 = 0;
            SD->field_1580 = SD->field_1584;
        }
    }
    if (changed) {
        m = SD->field_0044;
        x = m ? (u16)(m * (SD->field_1580 + 1)) >> 8 : 0;
        y = x ? (u16)(x * (SD->field_1586 + 1)) >> 8 : 0;
        func_80049F10(y, y);
    }
    if (SD->field_0512 != 0) {
        SD->cd_volume += SD->field_0512;
        if (SD->cd_volume <= SD->field_0049 && SD->field_0512 < 0) {
            SD->field_0512 = 0;
            SD->cd_volume = SD->field_0049;
        }
        if (SD->cd_volume >= SD->field_0049 && SD->field_0512 > 0) {
            SD->field_0512 = 0;
            SD->cd_volume = SD->field_0049;
        }
        y = SD->cd_volume;
        func_80044DC0(y);
    }
}
```

### Register pins do not reach the product

The entry identifies the mechanism precisely -- the product pseudo has one
register use, so regclass prefers `LO_REG` and reload materialises it through
the first free caller-saved register -- and notes that no spelling gives it a
second use for free. Pinning is the obvious remaining lever and it does not
work either, for a reason worth recording.

A `register s32 prod asm("$2")` cannot be applied without first lifting the
product out of the ternary into its own statement, and that lift is what costs
the function: `prod = m * (SD->field_1580 + 1); x = m ? (u16)prod >> 8 : 0;`
builds 0x290 against 0x2A4 and 64 differing positions, because the multiply
now happens unconditionally on both arms. The pin is fine; the statement it
requires is not.

Pinning only the first result (`register u16 x asm("$4")`, the `$a0` the entry
wants) reaches 10, not 7: it fixes the three `x` positions and costs four
elsewhere, because `$a0` is then unavailable where the build currently uses it.

So the two halves cannot be taken separately. The product has to become a
`GR_REGS` pseudo through a second use inside the existing expression, and no
pin substitutes for that.
