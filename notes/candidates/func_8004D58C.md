## `func_8004D58C` at 0x8004D58C

`gcc_2_8_1_g0_no_sched1`, 116 instructions against a target of 116, opcode
distance 4, positional opcode diff 11.

Resets one model slot and rebuilds its bone-enable mask. The slot is
`D_800F2C40` at a stride of 0xE20, reached by the `x*8 - x`, shift, add, shift
chain rather than a multiply.

It clears two per-row tables, ten rows each: halfwords at +0x2C8 set to 0xFFFF
with a row pitch of 0x74, and halfwords at +0x750 zeroed with a row pitch of
0x76. The extra two bytes per row of the second table are its 59th entry, which
the outer loop clears separately through +0x7C4. Then it clears the eight mask
bytes at +0xBEC counting down, walks the owner's list, and for the two entries
it cares about copies a pair of words into +0xDD8 and +0xDE0.

## What reached distance 4 and the exact length

**Six values are initialised before the slot address is computed.** The target
opens with the null result pointer, then the second result pointer, the outer
counter, the 0xFFFF fill constant, and the two row offsets, five of them
emitted as copies of the first zero. Leaving those to their natural positions
puts them after the address chain and costs eleven positions.

**The inner loop walks byte offsets, not pointers.** The target recomputes
`base + offset` for both tables on every iteration and advances the offsets by
two, rather than incrementing two cursors. Written with cursors the loop is two
instructions short; written with offsets it lands on 116 exactly.

**The list is terminated by -1, not by null,** so the walk ends on a comparison
against that value with an initial null check guarding entry.

**The bone loop divides a signed index by eight** — the `bgez` plus `addiu 7`
before the `sra 3` is the rounding — and the mask byte is stored on both paths,
with only the OR inside the conditional.

## What is left

Four opcodes and eleven positions. Most of the differing words are one
consistent register permutation: the target holds the first result pointer in
`$t3` and the two row offsets in `$t5` and `$t4`, where this build assigns
`$t5` to the pointer and shifts the offsets down.

The two real placements left are delay slots. The target puts the +0x750 store
in the inner loop's branch delay slot, and advances the entry pointer by four in
the delay slot after loading the header; this build fills the first with the
second offset advance and leaves the second as a `nop`. Four orderings of the
inner loop body were swept against every profile: moving the counter increment
between the two stores is the best of them and is what is stored, worth four
words over the alternatives.

```c
#include "../types.h"

typedef struct Node {
    struct Node *next;
    u8 *data;
    s32 flag;
    u8 pad_0C[3];
    u8 kind;
} Node;

typedef struct {
    u8 pad_00[0x10];
    Node *head;
} Owner;

extern u8 D_800F2C40[];

void func_8004D58C(s32 slot, Owner *owner)
{
    u8 *base;
    u8 *row;
    Node *n;
    u8 *a;
    u8 *b;
    u8 *pa;
    u8 *pb;
    u8 *ent;
    u16 *hdr;
    s32 i;
    s32 j;
    s32 oa;
    s32 ob;
    s32 k;
    s32 q;
    s32 ff;
    s32 ca;
    s32 cb;
    u8 val;

    pa = 0;
    pb = 0;
    i = 0;
    ff = 0xFFFF;
    oa = 0;
    ob = 0;
    base = &D_800F2C40[slot * 0xE20];
    row = base;
    *(u16 *)(base + 0xE06) = 0;
    *(u16 *)(base + 0xE08) = 0;
    *(u32 *)(base + 0xDD8) = 0;
    *(u32 *)(base + 0xDDC) = 0;
    *(u32 *)(base + 0xDE0) = 0;
    *(u32 *)(base + 0xDE4) = 0;
    *(u32 *)(base + 0xDF0) = 0;
    do {
        *(u16 *)(row + 0x7C4) = 0;
        j = 0;
        ca = oa;
        cb = ob;
        do {
            a = base + ca;
            ca += 2;
            b = base + cb;
            cb += 2;
            *(u16 *)(b + 0x2C8) = ff;
            j++;
            *(u16 *)(a + 0x750) = 0;
        } while (j < 0x3A);
        oa += 0x76;
        ob += 0x74;
        i++;
        row += 0x76;
    } while (i < 0xA);
    k = 7;
    a = base + k;
    do {
        a[0xBEC] = 0;
        k--;
        a--;
    } while (k >= 0);
    n = owner->head;
    if (n == 0) {
        return;
    }
    do {
        if (n->flag != 0) {
            if (n->kind == 3) {
                pa = n->data;
            }
            if (n->kind == 2) {
                pb = n->data;
            }
        }
        n = n->next;
    } while (n != (Node *)-1);
    if (pa != 0) {
        pa += 8;
        hdr = *(u16 **)pa;
        pa += 4;
        if (hdr[0] != 0) {
            i = 0;
            ent = (u8 *)hdr;
            do {
                q = i / 8;
                b = base + q;
                val = b[0xBEC];
                if (*(u32 *)(ent + 4) & 0x100) {
                    val = val | (1 << (i - q * 8));
                }
                b[0xBEC] = val;
                i++;
                ent += 4;
            } while ((u32)i < (u32)hdr[0]);
        }
        *(u32 *)(base + 0xDD8) = *(u32 *)pa;
        *(u32 *)(base + 0xDDC) = *(u32 *)(pa + 4);
    }
    if (pb != 0) {
        pb += 4;
        *(u32 *)(base + 0xDE0) = *(u32 *)pb;
        *(u32 *)(base + 0xDE4) = *(u32 *)(pb + 4);
    }
}
```
