## `func_8005E808` at 0x8005E808

`gcc_2_8_1_g8`, 253 instructions against a target of 251, opcode distance 6,
97 differing positions. Written from scratch; there was no previous candidate,
and all six canonical attempts were the automatic first pass.

Recomputes a model's audible radius. The magnitude of the `s16` at `+0x20`
sets the base radius at `+0x22`, capped at 0x4000, and when that field is
negative the two 8-byte descriptors that follow the header are each scored,
with the largest result kept. Type 0x80/0x81 asks `func_80058DD8` whether the
slot named by the low seven bits is live, pulls its position with
`Model_CopySlotU16Values`, offsets it by the descriptor and measures the
straight distance to the matching entry in `D_800F5768`. Type 1 has
`func_8005EBF4` fill a ten-point path and sums its nine segment lengths. Type 4
scales a fixed 6284 per unit by the descriptor's second word. All three divide
by 1000 against the same `0x10624DD3` magic, which GCC materialises once and
spills.

**Levers, in the order they paid.**

- *The switch case bodies have to be written in the target's layout order,
  0x80/0x81 first, then 1, then 4.* GCC emits case bodies in source order, and
  the target's blocks run `.L8005E904` (0x80/0x81), `.L8005EA04` (1),
  `.L8005EAF4` (4). Writing them in numeric order costs 93 positions on its
  own, 223 down to 130, because every block lands at the wrong address and the
  dispatch tree inverts with it.

- *`if (a >= 0x4000) cap; else radius;` and not the other way round.* The
  target's fall-through is the 0x4000 store, so the source has to test the
  upper bound, not the lower one.

- *The compare-and-store tail is written out inside case 0x80/0x81 and shared
  by cases 1 and 4.* The target has two copies of
  `if ((u16)p[0x22] < d) p[0x22] = d;`: one inline at the end of the first
  case, and one that cases 1 and 4 reach, the second by falling through. A
  single copy after the `switch` makes all three cases jump to it and is 14
  positions worse; writing it in each case and letting cross-jumping merge the
  two adjacent ones reproduces the target. 130 down to 111.

- *The radius is unsigned.* The final comparison is `sltu`, so `d` has to be a
  `u32`; an `s32` gives `slt` and, in case 4, drops the unsigned `multu` that
  the `/ 4096000` needs.

- *The second scan loop needs a pointer for the previous point and array
  indexing for the current one.* The target reloads the spilled `&buf` each
  iteration and adds the running byte offset, then reads `0x0`, `0x2` and
  `0x4` off that one register. Spelling all three as
  `*(s16 *)((u8 *)buf + off + n)` folds each into its own `$sp`-relative
  address and costs three instructions. 111 down to 97.

- *The two descriptors are walked by three pointers, not one.* The target
  advances `e` (the descriptor), `q` (`e + 2`) and `g` (into `D_800F5768`)
  by 8 each iteration, and the giv that covers the `q` accesses is based at
  `e + 2`, which is what puts the switch selector at `0x4($s5)`. That falls
  out of `q = e + 2` being assigned at the top of the loop body and the last
  `q`-relative access in the body being at `q + 0`, in case 4.

**What is left** is six opcodes and a register permutation, in three clusters.

- *Case 4 addresses `D_800F5768 + 0x10` twice and the target materialises the
  base.* The target emits `lui $t0` / `addiu $t0, $t0, %lo` and then
  `0x10($t0)` for both the `lh` and the `lhu`; this build emits the assembler
  macro form twice, `lui` plus the load, for the same four instructions but
  one `lui` too many and one `addiu` too few. A `u8 *` local assigned
  `D_800F5768` does not survive: GCC constant-propagates it back into both
  memory references. Assigning that local outside the loop instead is 11
  positions worse.

- *Case 4's absolute value duplicates its `mult` into the branch delay slot.*
  The target leaves that slot empty and issues one `mult $a0, $v0` after the
  join; reorg here steals the join's `mult` into the slot and repeats it on
  the negated path. Using a variable of its own for the absolute value moves
  the register but not the duplication.

- *`p` and `q` are swapped, `$s4` against `$s5`.* Everything else in the
  register assignment matches, `e` in `$fp`, `g` in `$s6`, `i` in `$s7`, the
  loop temporaries in `$s0` to `$s3`, and the three spill slots at 0x60, 0x64
  and 0x68 hold the same values as the target's. Most of the 97 positions are
  this one swap.

Profiles crossed: `gcc_2_8_1_g8` (recorded), `gcc_2_8_1_g0` and
`gcc_2_8_1_cc_g0_as_g8_no_split` at 98, `gcc_2_8_1_g8_split` at 245.

```c
#include "../types.h"

extern u8 *D_8009B074;
extern u8 D_800F5768[];

extern void func_8005FB30(u8 *);
extern s32 func_80058DD8(s32);
extern void Model_CopySlotU16Values(s32, void *);
extern s32 SquareRoot0(s32);
extern void func_8005EBF4(u8 *, s32, s32, s32, void *);

/* Recomputes a model's audible radius. The magnitude of the s16 at +0x20 sets
   the base radius at +0x22, capped at 0x4000, and when that field is negative
   the two 8-byte descriptors that follow the header are each scored and the
   largest result wins: type 0x80/0x81 measures the straight distance from the
   descriptor's offset position to the matching entry in D_800F5768, type 1
   sums the segment lengths of a ten-point path filled in by func_8005EBF4,
   and type 4 scales a fixed 6284 per unit by the descriptor's second word. */
void func_8005E808(u8 *p)
{
    s16 pos[3];
    s16 buf[10][3];
    u8 *e;
    u8 *q;
    u8 *g;
    s32 i;
    s32 j;
    s32 off;
    s32 a;
    s32 k;
    s32 sum;
    s32 t;
    u32 d;
    s32 dx;
    s32 dy;
    s32 dz;
    s32 slot;
    u8 *prev;
    s32 n;
    u16 v;

    n = *(s16 *)(p + 0x20);
    a = n;
    if (n < 0) {
        a = -a;
    }
    t = a;
    if (a <= 0) {
        t = 1;
    }
    k = t * 2;
    func_8005FB30(p);
    *(s16 *)(p + 0x24) = 0;
    if (D_8009B074[0x26] != 0) {
        return;
    }
    p[0x26] = 1;
    if (a >= 0x4000) {
        *(s16 *)(p + 0x22) = 0x4000;
    } else {
        *(s16 *)(p + 0x22) = k;
    }
    if (*(s16 *)(p + 0x20) >= 0) {
        return;
    }

    e = p;
    g = D_800F5768;
    for (i = 0; i < 2; i++, e += 8, g += 8) {
        q = e + 2;
        sum = 0;
        switch (*(s16 *)(q + 4)) {
        case 0x80:
        case 0x81:
            slot = (s16)(*(u16 *)(q + 4) & 0xFF7F);
            if (func_80058DD8(slot) != 1) {
                continue;
            }
            Model_CopySlotU16Values(slot, pos);
            pos[0] = pos[0] + *(u16 *)e;
            pos[1] = pos[1] + *(u16 *)q;
            pos[2] = pos[2] + *(u16 *)(q + 2);
            dx = pos[0] - *(s16 *)g;
            dy = pos[1] - *(s16 *)(g + 2);
            dz = pos[2] - *(s16 *)(g + 4);
            d = k * SquareRoot0(dx * dx + dy * dy + dz * dz) / 1000;
            if (*(u16 *)(p + 0x22) < d) {
                *(s16 *)(p + 0x22) = d;
            }
            continue;
        case 1:
            for (j = 0; j < 10; j++) {
                func_8005EBF4(p, i, j, 0x1E, buf[j]);
            }
            off = 0;
            for (j = 1; j < 10; j++) {
                prev = (u8 *)buf + off;
                dx = *(s16 *)prev - buf[j][0];
                dy = *(s16 *)(prev + 2) - buf[j][1];
                dz = *(s16 *)(prev + 4) - buf[j][2];
                sum += SquareRoot0(dx * dx + dy * dy + dz * dz);
                off += 6;
            }
            d = k * sum / 1000;
            break;
        case 4:
            v = *(u16 *)(D_800F5768 + 0x10);
            if (*(s16 *)(D_800F5768 + 0x10) < *(s16 *)e) {
                v = *(u16 *)e;
            }
            t = (s16)v * 6284 / 1000;
            t = k * t;
            slot = *(s16 *)q;
            if (slot < 0) {
                slot = -slot;
            }
            d = (u32)(t * slot) / 4096000;
            break;
        default:
            continue;
        }
        if (*(u16 *)(p + 0x22) < d) {
            *(s16 *)(p + 0x22) = d;
        }
    }
}
```
