## `func_80049FB4` at 0x80049FB4

`gcc_2_8_1_g8_split`, 84 instructions against a target of 82, opcode
distance 2.

Reads one bit-run length out of the `D_80010834` table. The two coordinate
arguments are summed and taken as `s16`; the high bits pick a table row and the
low seven a column. A second `s16` difference then selects between two paths
that index the same table twelve columns apart and shift the fetched halfword
in opposite directions, masking the result to 16 bits.

`0x2AAAAAAB` is the signed divide-by-twelve magic, so every `mult`/`mfhi`/`sra`
group in the listing is `/ 12` written plainly; `sra 7` after a conditional
`+0x7F` is `/ 128`. Writing both as ordinary C division reproduces all three
sequences, and no part of this function needs the magic constant spelled out.

The caller `src/game/sound_voice_setup.c` already declares the signature.
`D_80010834` had no declaration and is taken as `u8 *` with byte offsets, since
the row stride is `0x100` bytes and the column step is 2.

**Negate the `s16` difference, not the expression behind it.** The negative
path needs `-d` where `d` is the already-narrowed `s16`; recomputing
`(arg0 + row) - arg2` and negating that costs an extra `addu`, because GCC
re-derives the sum instead of reusing the narrowed value. That is the whole
difference between distance 3 and distance 2.

What remains is two extra `nop` and nothing missing, so the instruction
sequence is otherwise exact and only two delay slots are padded where retail
fills them. Recall that `nop` is `sll $zero, $zero, 0`, so an opcode census
reports these as extra `sll`.

Crossed without improving on 2: the column as `t - row * 128` and as
`t - (row << 7)`, the difference carried as `s32` before narrowing, and three
profiles. `gcc_2_8_1_g0` reaches the exact 82 instructions but at distance 4,
so the shorter build is the worse base.

```c
#include "../../src/types.h"

extern u8 D_80010834[];

s32 func_80049FB4(s32 arg0, s32 arg1, s32 arg2, s32 arg3)
{
    s16 t;
    s16 d;
    s16 n;
    s16 r;
    s32 row;
    s32 col;
    s32 q;

    t = (s16)(arg1 + arg3);
    row = t / 128;
    col = t - row * 128;
    d = (s16)((arg0 + row) - arg2);
    if (d < 0) {
        n = (s16)(-d);
        r = (s16)(n - (n / 12) * 12);
        q = (12 - r) % 12;
        return (*(u16 *)(D_80010834 + col * 2 + (q << 8)) >>
                ((n + 11) / 12)) & 0xFFFF;
    }
    q = d / 12;
    r = (s16)(d - q * 12);
    return (*(u16 *)(D_80010834 + col * 2 + (r << 8)) << q) & 0xFFFF;
}
```
