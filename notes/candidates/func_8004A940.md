## `func_8004A940` at 0x8004A940

`gcc_2_8_1_cc_g0_as_g8`, 109 instructions against a target of 111, opcode
distance 10, positional opcode diff 16.

Sound-driver voice allocation. `D_8009B458` points at a table of 0x28-byte
voice records whose count sits at +0x510. The function tries five strategies in
order and hands the chosen slot to `func_8004A8E4`.

First it scans for a record whose byte at +0x183 is 0x63 and abandons the call
outright if one exists. Then it scans for a free record, byte at +0x18D zero,
and takes it. Then it looks for a record already holding this key and subkey, at
+0x183 and +0x185. Then it repeats the free scan. Failing all of those it takes
the record with the smallest halfword at +0x19E, and gives up only if that
minimum is still 0xFFFF.

Two facts are already pinned.

**`func_8004A8E4` is called with two arguments but the matched definition in
`src/game/` declares one.** The call site sets `$a1` from the key byte, so the
declaration here has to be prototype-less for the second argument to be passed
at all.

**The third scan re-reads the global on every iteration** where the others hoist
it. The target reloads `D_8009B458` and the count inside that loop and walks the
records by an accumulating byte offset, which is why it carries an `addu` of a
separate offset register rather than advancing the record pointer directly.

## What is left

Two instructions and sixteen positions. The count is copied into its own
register before three of the five loops in the target and the loop counter is
zeroed in the branch delay slot; this build lets GCC place both. The `0xFFFF`
minimum is also initialised before the fifth loop's guard rather than after it.

Reordering the three opening statements was swept across every profile: putting
the key copy between the global load and the count read is the best of them and
is what is stored, worth one position over the alternatives.

```c
#include "../types.h"

extern u8 *D_8009B458;
extern s32 func_8004A8E4();

void func_8004A940(s32 key, s32 sub)
{
    u8 *p;
    s32 i;
    s32 n;
    s32 best;
    s32 off;
    u32 minv;
    s32 k;

    best = -1;
    p = D_8009B458;
    k = key;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        i = 0;
        do {
            if (p[0x183] == 0x63) {
                return;
            }
            i++;
            p += 0x28;
        } while (i < n);
    }
    p = D_8009B458;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        i = 0;
        do {
            if (p[0x18D] == 0) {
                goto call;
            }
            i++;
            p += 0x28;
        } while (i < n);
        p = D_8009B458;
        n = *(s16 *)(p + 0x510);
    }
    if (n > 0) {
        i = 0;
        off = 0;
        do {
            if (p[off + 0x183] == (u8)k && p[off + 0x185] == (u8)sub) {
                goto call2;
            }
            p = D_8009B458;
            i++;
            off += 0x28;
        } while (i < *(s16 *)(p + 0x510));
    }
    p = D_8009B458;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        i = 0;
        do {
            if (p[0x18D] == 0) {
                goto call;
            }
            i++;
            p += 0x28;
        } while (i < n);
        p = D_8009B458;
    }
    minv = 0xFFFF;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        u8 *q = p;
        i = 0;
        do {
            u32 v = *(u16 *)(q + 0x19E);
            if (v < minv) {
                minv = v;
                best = i;
            }
            i++;
            q += 0x28;
        } while (i < n);
    }
    if (0xFFFE < minv) {
        return;
    }
    i = best;
call:
    sub = (u8)k;
call2:
    func_8004A8E4(i, sub);
}
```
