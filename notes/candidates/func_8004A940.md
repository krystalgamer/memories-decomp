## `func_8004A940` at 0x8004A940

`gcc_2_8_1_cc_g0_as_g8`, 115 instructions against a target of 111, opcode
distance 4, positional opcode diff 16.

Sound-driver voice allocation. `D_8009B458` points at a table of 0x28-byte
voice records whose count sits at +0x510. The function tries five strategies in
order and hands the chosen slot to `func_8004A8E4`.

First it scans for a record whose byte at +0x183 is 0x63 and gives up if one
exists. Then it scans for a free record, byte at +0x18D zero, and takes it.
Then it looks for a record already holding this key and subkey, at +0x183 and
+0x185. Then it repeats the free scan. Failing all of those it takes the record
with the smallest halfword at +0x19E, and gives up only if that minimum is
still 0xFFFF.

## What moved it from distance 6

**The function returns a value.** Two branch delay slots carry it and a `void`
signature throws both away. The 0x63 scan exits with `$v0` set to the loop
index, and the give-up path after the minimum scan exits with `$v0` set to -1.
Declaring the function `s32` and returning the index and -1 from those two
points recovers both, and the tail call to `func_8004A8E4` supplies the value on
the remaining path.

**The loop counter is zeroed before the bound is copied.** All five guards fill
their `blez` delay slot with `addu $a0, $zero, $zero`, so the counter zeroing
has to precede the count copy inside the guard rather than follow it.

**Four of the five loops copy the count into their own register**, and the
third is the exception: it reloads `D_8009B458` and the count every iteration
and walks by an accumulating byte offset, so it keeps `addu $a2, $a0, $zero` to
zero that offset where the others copy the count.

**`func_8004A8E4` is called with two arguments while the matched definition
declares one,** so the declaration here has to be prototype-less.

## The length trade-off, stated plainly

This form is four instructions longer than the target, where the previously
stored form was two shorter. Opcode distance improved from 6 to 4 and
positional diff from 18 to 16, so it is kept as the better base, but the length
moved the wrong way and that is not yet explained. Three unfilled delay slots
account for most of it: the target fills them and this build emits `nop`.

## What is left

Four opcodes and sixteen positions. The target copies the key parameter into a
register as its second instruction, before the global load, and hoists the 0x63
comparison constant ahead of the first loop; this build emits both later. The
third loop also swaps the counter increment and the count reload.

Swept without effect on this base, after the return-value change made the axis
live again: all four orderings of the opening key copy, global load, best
initialisation and count read, against every profile. They tie exactly.

```c
#include "../types.h"

extern u8 *D_8009B458;
extern s32 func_8004A8E4();

s32 func_8004A940(s32 key, s32 sub)
{
    u8 *p;
    s32 i;
    s32 n;
    s32 best;
    s32 off;
    u32 minv;
    s32 m;
    s32 k;

    best = -1;
    p = D_8009B458;
    k = key;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        i = 0;
        m = n;
        do {
            if (p[0x183] == 0x63) {
                return i;
            }
            i++;
            p += 0x28;
        } while (i < m);
    }
    p = D_8009B458;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        i = 0;
        m = n;
        do {
            if (p[0x18D] == 0) {
                goto call;
            }
            i++;
            p += 0x28;
        } while (i < m);
        p = D_8009B458;
        n = *(s16 *)(p + 0x510);
    }
    if (n > 0) {
        i = 0;
        m = n;
        off = 0;
        do {
            if (p[off + 0x183] == (u8)k && p[off + 0x185] == (u8)sub) {
                goto call2;
            }
            p = D_8009B458;
            i++;
            off += 0x28;
        } while (i < *(s16 *)(p + 0x510));
    }
    p = D_8009B458;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        i = 0;
        m = n;
        do {
            if (p[0x18D] == 0) {
                goto call;
            }
            i++;
            p += 0x28;
        } while (i < m);
        p = D_8009B458;
    }
    minv = 0xFFFF;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        u8 *q = p;
        i = 0;
        do {
            u32 v = *(u16 *)(q + 0x19E);
            if (v < minv) {
                minv = v;
                best = i;
            }
            i++;
            q += 0x28;
        } while (i < m);
    }
    if (0xFFFE < minv) {
        return -1;
    }
    i = best;
call:
    sub = (u8)k;
call2:
    return func_8004A8E4(i, sub);
}
```
