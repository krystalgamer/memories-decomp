## `func_8004A940` at 0x8004A940

`gcc_2_8_1_cc_g0_as_g8`, 113 instructions against a target of 111, opcode
distance 6, positional opcode diff 18.

Sound-driver voice allocation. `D_8009B458` points at a table of 0x28-byte
voice records whose count sits at +0x510. The function tries five strategies in
order and hands the chosen slot to `func_8004A8E4`.

First it scans for a record whose byte at +0x183 is 0x63 and abandons the call
outright if one exists. Then it scans for a free record, byte at +0x18D zero,
and takes it. Then it looks for a record already holding this key and subkey, at
+0x183 and +0x185. Then it repeats the free scan. Failing all of those it takes
the record with the smallest halfword at +0x19E, and gives up only if that
minimum is still 0xFFFF.

## What moved it from distance 10

**Four of the five loops copy the count into their own register before the
guard.** The target emits `addu $a2, $v0, $zero` and its equivalents in loops
one, two, four and five, then compares the counter against that copy rather
than against the loaded halfword. Assigning the loaded count to a second local
inside each guard reproduces those copies and takes opcode distance from 10
to 6.

**The third loop is the exception and must stay as a re-read.** It reloads
`D_8009B458` and the count on every iteration and walks the records by an
accumulating byte offset, which is why it carries a separate offset register
rather than advancing the record pointer. Applying the bound copy there as well
is wrong.

**`func_8004A8E4` is called with two arguments while the matched definition
declares one,** so the declaration here has to be prototype-less for the second
argument to be passed at all.

## What is left

Two instructions and eighteen positions. The target copies the key parameter
into a register as its second instruction, before the global load, and hoists
the 0x63 comparison constant ahead of the first loop; this build emits both
later. It also fills three guard delay slots with the counter zeroing where
this build leaves a `nop`.

Swept without effect on this base: all three orderings of the opening key copy,
global load and count read, and hoisting 0x63 into its own local, each against
every profile. They tie exactly, so that placement is not reachable by
statement order here.

## Alternative with fewer positions

Dropping the four bound copies gives 109 instructions and positional diff 16,
but opcode distance 10. That is the form previously stored. Its positional diff
is lower, but the opcode multiset is far worse and the four copies are plainly
visible in the target, so the form stored here is the better base.

```c
#include "../types.h"

extern u8 *D_8009B458;
extern s32 func_8004A8E4();

void func_8004A940(s32 key, s32 sub)
{
    u8 *p;
    s32 i;
    s32 n;
    s32 best;
    s32 off;
    u32 minv;
    s32 m;
    s32 k;

    best = -1;
    p = D_8009B458;
    k = key;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        m = n;
        i = 0;
        do {
            if (p[0x183] == 0x63) {
                return;
            }
            i++;
            p += 0x28;
        } while (i < m);
    }
    p = D_8009B458;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        m = n;
        i = 0;
        do {
            if (p[0x18D] == 0) {
                goto call;
            }
            i++;
            p += 0x28;
        } while (i < m);
        p = D_8009B458;
        n = *(s16 *)(p + 0x510);
    }
    if (n > 0) {
        m = n;
        i = 0;
        off = 0;
        do {
            if (p[off + 0x183] == (u8)k && p[off + 0x185] == (u8)sub) {
                goto call2;
            }
            p = D_8009B458;
            i++;
            off += 0x28;
        } while (i < *(s16 *)(p + 0x510));
    }
    p = D_8009B458;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        m = n;
        i = 0;
        do {
            if (p[0x18D] == 0) {
                goto call;
            }
            i++;
            p += 0x28;
        } while (i < m);
        p = D_8009B458;
    }
    minv = 0xFFFF;
    n = *(s16 *)(p + 0x510);
    if (n > 0) {
        u8 *q = p;
        i = 0;
        do {
            u32 v = *(u16 *)(q + 0x19E);
            if (v < minv) {
                minv = v;
                best = i;
            }
            i++;
            q += 0x28;
        } while (i < m);
    }
    if (0xFFFE < minv) {
        return;
    }
    i = best;
call:
    sub = (u8)k;
call2:
    func_8004A8E4(i, sub);
}
```
