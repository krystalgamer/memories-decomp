#ifndef MEMORIES_DECOMP_FUNC_80033500_H
#define MEMORIES_DECOMP_FUNC_80033500_H

#include "card_list_text_boxes.h"

/* Returns the id of the entry the cursor is on, or 0 when that row's flag
   byte is clear. */
s32 func_80033500(CardList *list);

#endif
