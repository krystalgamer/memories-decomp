#ifndef MEMORIES_DECOMP_FUNC_8002596C_H
#define MEMORIES_DECOMP_FUNC_8002596C_H

#include "../types.h"

/* gDuelEffect_apfnGroupHandler entry: latches its first frame through
 * DuelEffect_MarkInitialized, then walks the acting side's grid records at
 * D_801A7AD8 spawning the effect objects for the cards it finds. */
void func_8002596C(void);

#endif
