diff --git a/maspsx/__init__.py b/maspsx/__init__.py
index a45d7cc..a93ab28 100644
--- a/maspsx/__init__.py
+++ b/maspsx/__init__.py
@@ -475,6 +475,7 @@ class MaspsxProcessor:
         self.sdata_entries: dict[str, int] = {}
 
         self.comm_symbols: set[str] = set()
+        self.small_extern_symbols: set[str] = set()
 
     def preprocess_lines(self) -> None:
         in_sdata = False
@@ -518,6 +519,9 @@ class MaspsxProcessor:
 
             if line.startswith(".extern"):
                 in_sdata = False
+                match = re.match(r"\.extern\s+([^,\s]+)\s*,\s*(\d+)", line)
+                if match and int(match.group(2)) <= self.sdata_limit:
+                    self.small_extern_symbols.add(match.group(1))
                 continue
 
             if line.startswith(".comm") or line.startswith(".lcomm"):
@@ -579,6 +583,7 @@ class MaspsxProcessor:
         self.bss_entries = {}
         self.sbss_entries = {}
         self.sdata_entries = {}
+        self.small_extern_symbols = set()
 
         self.preprocess_lines()
 
@@ -685,7 +690,9 @@ class MaspsxProcessor:
                     gp_allowed = True
 
                 if gp_allowed and (
-                    symbol in self.sbss_entries or symbol in self.sdata_entries
+                    symbol in self.sbss_entries
+                    or symbol in self.sdata_entries
+                    or symbol in self.small_extern_symbols
                 ):
                     return True
 
diff --git a/tests/test_div.py b/tests/test_div.py
index 762ec26..c7715e4 100644
--- a/tests/test_div.py
+++ b/tests/test_div.py
@@ -318,6 +318,71 @@ class TestDivExpansion(unittest.TestCase):
         clean_lines = strip_comments(res)
         self.assertEqual(expected_lines, clean_lines)
 
+    def test_div_expand_external_sdata_nop(self):
+        lines = [
+            "rem	$2,$3,$2",
+            "sh	$2,gUpdateRate",
+            ".extern	gUpdateRate, 2",
+        ]
+        expected_lines = [
+            ".set	noat",
+            "div	$zero,$3,$2",
+            "bnez	$2,.L_NOT_DIV_BY_ZERO_0",
+            "nop",
+            "break	0x7",
+            ".L_NOT_DIV_BY_ZERO_0:",
+            "addiu	$at,$zero,-1",
+            "bne	$2,$at,.L_DIV_BY_POSITIVE_SIGN_0",
+            "lui	$at,0x8000",
+            "bne	$3,$at,.L_DIV_BY_POSITIVE_SIGN_0",
+            "nop",
+            "break	0x6",
+            ".L_DIV_BY_POSITIVE_SIGN_0:",
+            "mfhi	$2",
+            ".set	at",
+            "nop",
+            "sh	$2,gUpdateRate",
+            ".extern	gUpdateRate, 2",
+        ]
+
+        mp = MaspsxProcessor(lines, sdata_limit=8, expand_div=True)
+        res = mp.process_lines()
+
+        clean_lines = strip_comments(res)
+        self.assertEqual(expected_lines, clean_lines)
+
+    def test_div_expand_external_data_no_nop(self):
+        lines = [
+            "rem	$2,$3,$2",
+            "sh	$2,gUpdateRate",
+            ".extern	gUpdateRate, 16",
+        ]
+        expected_lines = [
+            ".set	noat",
+            "div	$zero,$3,$2",
+            "bnez	$2,.L_NOT_DIV_BY_ZERO_0",
+            "nop",
+            "break	0x7",
+            ".L_NOT_DIV_BY_ZERO_0:",
+            "addiu	$at,$zero,-1",
+            "bne	$2,$at,.L_DIV_BY_POSITIVE_SIGN_0",
+            "lui	$at,0x8000",
+            "bne	$3,$at,.L_DIV_BY_POSITIVE_SIGN_0",
+            "nop",
+            "break	0x6",
+            ".L_DIV_BY_POSITIVE_SIGN_0:",
+            "mfhi	$2",
+            ".set	at",
+            "sh	$2,gUpdateRate",
+            ".extern	gUpdateRate, 16",
+        ]
+
+        mp = MaspsxProcessor(lines, sdata_limit=8, expand_div=True)
+        res = mp.process_lines()
+
+        clean_lines = strip_comments(res)
+        self.assertEqual(expected_lines, clean_lines)
+
     def test_nop_div_mult(self):
         """
         mult and mflo/mhfi must be separated by at least two instructions
